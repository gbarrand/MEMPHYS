
bush_home=
if [ -d ../../bush ] ; then
  bush_home="`pwd`/../../bush"
else
  if [ -d ../../../softinex/bush ] ; then
    bush_home="`pwd`/../../../softinex/bush"
  fi
fi

if [ ! -d ${bush_home} ] ; then
  echo "directory ${bush_home} not found."
  exit
fi
