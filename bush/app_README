
 To build :
   OS> cd <app>/mgr
   OS> ./build
 ( OS> ./build -v)

 To run :
   OS> cd <app>/mgr
 the OSX Cocoa version :
   Darwin> open ./bin_sdk_mac/<distrib>/<app>/<version>/<app>.app
 the Linux X11 version :
   Linux> ./bin_gnu/<distrib>/<app>/<version>/bin/<app>
 the Windows version :
   cygwin> ./bin_visaul/<distrib>/<app>/<version>/bin/<app>.exe

